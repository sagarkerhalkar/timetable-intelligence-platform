# 2026-08-13 — Emergency recovery: 8 source registrations + stale v1.0.11.1 web build

## Real Windows evidence

The uploaded `TIMETABLE_CORE_RECOVERY_20260813_162409.txt` proved the prior DB recovery was only partially successful from a user-experience perspective.

- service/API authoritative DB: 4 source registrations;
- newer/root DB: 8 source registrations;
- all four trusted timetable source sync requests returned HTTP 200;
- Today API contained 12 timetable rows;
- Dashboard, Sheet Updates, Test Monitor and QR endpoints returned HTTP 200;
- all final page probes returned HTTP 200.

Despite that, screenshots immediately afterward showed `Installed build: v1.0.11.1`, Google Sheets `0 of 0`, and Today `0 classes`.

## Root cause

Two independent recovery gaps remained:

1. only the four timetable source registrations were retained in the authoritative DB while four newer additional source registrations remained only in the newer/root DB;
2. the prior `RESTORE_LAST_WORKING_APP.ps1` restored an old `.next` production build. The acceptance gate only checked page HTTP status, so an HTTP 200 stale page could falsely pass while showing old/empty data.

## Emergency v1.0.25.3 contract

- Back up both DBs, `.next`, `.env.local`, and changed web source.
- Stop only ports 3500/3550; never touch 3457.
- Merge all source registrations from root DB into the authoritative service/API DB while preserving existing core source IDs.
- Guarantee Humanities, Science, Commerce and Nirmaan registrations.
- Do not copy timetable/content/test/change/notification tables from root DB.
- Do not modify QR tables.
- Pin absolute `DATABASE_URL` to `services/api/data/timetable.db`.
- Sync the four timetable workbooks first, then every additional enabled registered workbook.
- Require current-day timetable API to contain rows.
- Apply known later cumulative Today/Sheet Updates/Test Monitor/navigation web source.
- Delete stale `.next`; run the real Windows `npm run typecheck` and `npm run build`; start only the fresh build.
- Check Today, Weekly, Google Sheets, Sheet Updates, Test Monitor and My QR Codes.

## Local validation

- Python helper compile: PASS.
- Synthetic source merge: 4 core + 8 newer -> 8 authoritative source rows: PASS.
- Existing core source IDs preserved: PASS.
- Synthetic QR table unchanged: PASS.
- PowerShell delimiter audit: PASS.
- Protected-port guard: PASS.

## Package

`timetable-emergency-recovery-v1.0.25.3.zip`

SHA-256: `29487a0ecfecc9bcb24b0a8fe49ff99368bf3a04b2611d375c1c1542a15880ec`

Run `RUN_EMERGENCY_RECOVER_ALL_SHEETS_NOW.cmd`. The real Windows result is authoritative. Do not merge the QR release candidate or install another QR update until this recovery is verified.