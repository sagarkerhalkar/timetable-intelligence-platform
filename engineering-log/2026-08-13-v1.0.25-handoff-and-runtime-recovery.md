# 2026-08-13 — v1.0.25 Handoff Repair and Mandatory GitHub Continuity

## User requirement reinforced today

The user explicitly required that all project development context be kept in GitHub so a future chat does not need the previous chat. This includes requirements, errors, successes, fixes and continuation state.

This requirement is now permanent and is formalized in `docs/GITHUB_MANDATORY_HANDOFF_POLICY.md`.

## Problem discovered

The v1.0.25 release-candidate documentation said the following runtime files were changed:

- `apps/web/app/qr/codes/page.tsx`
- `apps/web/app/qr/templates/page.tsx`
- `apps/web/app/qr/bulk/page.tsx`
- `apps/web/app/qr/stats/page.tsx`
- `apps/web/app/globals.css`

However, the public `release-source/v1.0.25/` tree contained only the shared pagination component/utility/tests and documentation, not all exact Windows runtime page source.

This meant GitHub was not sufficient by itself to reproduce the promised v1.0.25 UI and a later session had to recover the exact current Windows source.

**Result:** FAIL — handoff completeness.

## Corrective action

A read-only source collector was created to copy only the required current source from `D:\timetable-intelligence-platform` without modifying the application, databases or services and without touching protected port 3457.

The user ran it and supplied the generated runtime-source ZIP on 2026-08-13.

## Security check on recovered runtime source

The supplied source package was checked before treating it as a public-repository source base.

The recovery package contained source/manifest material only for the required runtime areas. No database, `.env`, credentials, private Sheet data, recipient data or obvious secret assignments were intentionally included in the sanitized source path used for continuation.

Sensitive/public-repository exclusions remain mandatory.

**Result:** PASS — sanitized source suitable as engineering base.

## v1.0.25 product requirement retained

v1.0.25 remains a focused QR UX release. Existing tracking, analytics, Wi-Fi, identity, template semantics and timetable behavior must remain unchanged.

Required page contract:

- My QR Codes: 8 large cards/page.
- Real QR preview on every card.
- Large `Show QR` modal.
- Source/destination link plus `Copy Source Link`.
- Tracked/public QR link plus `Copy QR Link`.
- Clipboard fallback for LAN/older browsers.
- Checkbox selection across pages.
- `Select this page`.
- `Clear selection`.
- `Delete Selected` with selected count.
- Multi-delete must reuse the established individual QR DELETE route in bounded groups.
- Templates: 6/page.
- Bulk template chooser: 6/page.
- Bulk Name + URL input: 10 rows/page.
- Bulk generated results: 10/page.
- Device Intelligence: 6/page.
- Recent verified scan history: 15/page.

Existing Analytics/Edit/PNG/SVG/PDF/activate/deactivate/clear-data/channel actions remain.

## Release safety rules retained

- Stable branch: `v1`.
- Release-candidate branch: `v1.0.24-qr-templates-bulk-analytics`.
- Draft PR: `#12`.
- Stable `v1` must not be changed until real Windows acceptance passes without rollback.
- Windows target: `D:\timetable-intelligence-platform`.
- Web port: 3500.
- API port: 3550.
- Port 3457 is protected and must not be touched.
- Do not weaken tests to make a release pass.

## Validation evidence carried forward

Earlier v1.0.24 Windows evidence:

- backend: 105/105 PASS;
- web regressions: 16/16 PASS;
- then strict project TypeScript failed with four TS2532 errors in Bulk parsing because `strict` + `noUncheckedIndexedAccess` were enabled.

Root cause: pre-package validation was weaker than the real project TypeScript gate.

Fix: move parsing to typed `qr-bulk.ts`, add parser regression tests and require semantic TypeScript validation using both `strict=true` and `noUncheckedIndexedAccess=true`.

**Result after source-level correction:** PASS for the corrected parser/source gate; Windows installer remains authoritative.

## Today’s process failure and fix

### Failure

A later chat initially could not supply the true complete v1.0.25 runtime implementation directly from GitHub because exact runtime page source was missing from the repository.

### Root cause

Previous handoff committed implementation notes and shared pagination source but did not commit all exact Windows runtime source needed to reproduce the release.

### Fix

1. Recover exact current Windows runtime source using a read-only collector.
2. Make GitHub engineering continuity mandatory.
3. Record requirements, FAIL/PASS evidence, root causes and next action in chronological engineering logs.
4. Require actual sanitized release source or deterministic package regeneration instructions in GitHub before a development session is considered handed off.

## Mandatory GitHub continuity rule added

New permanent file:

- `docs/GITHUB_MANDATORY_HANDOFF_POLICY.md`

It requires every future engineering session to write the following to GitHub before ending:

- requirements and changes;
- preserved scope;
- architecture decisions;
- changed files;
- commands/tests;
- every meaningful FAIL;
- root cause;
- fix;
- PASS result;
- build/typecheck/backend/frontend/runtime state;
- installer and rollback state;
- package/checksum;
- branch/PR/commit;
- unresolved blockers;
- exact next action;
- lessons learned.

Secrets/private data remain excluded because the repository is public.

## Current acceptance status

v1.0.25 must still be treated as **release candidate / not Windows-proven** until the complete real Windows installer gate passes without rollback.

The complete acceptance gate must include:

- complete backend suite;
- complete functional frontend regression suite;
- real project `tsc --noEmit`;
- `strict=true` and `noUncheckedIndexedAccess=true` compliance;
- full Next.js production build;
- main-page/runtime smoke checks;
- QR lifecycle;
- templates;
- bulk creation;
- independent per-QR analytics;
- v1.0.25 pagination/UI contract;
- timetable existing behavior;
- Test Monitor existing behavior;
- Sheet Updates existing behavior;
- rollback on any blocking failure.

## Exact next action

Continue completing and publishing the actual recovered v1.0.25 runtime source and deterministic installer/package material on PR #12, then run the resulting installer on the real Windows installation. Record every Windows FAIL/PASS result in a new chronological `engineering-log/` entry before considering merge to stable `v1`.
